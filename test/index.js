const fs = require('fs')
const assert = require('assert')
const copy = require('../lib')

/* 
describe('copy', function() {
  // todo
}) */

/* 
copy('imgs', 'statics/', (err, files, index) => {
  if (err) {
    console.error(err)
  }
}) */


console.log('test todo ...')
